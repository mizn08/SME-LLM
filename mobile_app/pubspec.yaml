name: bnpl_advisor_mobile
description: BNPL Advisor for SMEs — APC2026 Flutter client
publish_to: "none"
version: 1.0.0+1

environment:
  sdk: ">=3.3.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter
  cupertino_icons: ^1.0.8
  provider: ^6.1.2
  dio: ^5.7.0
  fl_chart: ^0.69.0
  shared_preferences: ^2.3.3
  file_picker: ^8.1.4
  intl: ^0.19.0
  share_plus: ^10.1.2
  local_auth: ^2.3.0
  pdf: ^3.11.1
  path_provider: ^2.1.5
  cross_file: ^0.3.4
  url_launcher: ^6.3.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/sample_transactions.csv
    - assets/images/app_logo.png
    - assets/datasets/
