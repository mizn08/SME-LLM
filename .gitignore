# Environment & secrets
.env
*.env.local

# Python
__pycache__/
*.py[cod]
.venv/
venv/
.pytest_cache/
.mypy_cache/
*.egg-info/

# Local databases
*.db
bnpl_local.db
backend/bnpl_local.db
backend/bnpl_local.db.*
backend/*.corrupt.bak

# Local uploads / vector store (regenerated at runtime)
backend/data/uploads/
backend/data/chroma/

# Flutter SDK (install separately; do not commit ~1GB SDK)
flutter/

# Flutter / Dart build outputs
mobile_app/.dart_tool/
mobile_app/build/
mobile_app/.flutter-plugins
mobile_app/.flutter-plugins-dependencies
mobile_app/.packages
mobile_app/.pub-cache/
mobile_app/.pub/
mobile_app/**/GeneratedPluginRegistrant.java

# Android local SDK path (machine-specific)
mobile_app/android/local.properties

# IDE
.idea/
*.iml
.vscode/
*.swp

# OS
.DS_Store
Thumbs.db

# Docker volumes (not in repo)
postgres_data/

# Optional: large APK (uncomment to exclude; upload via GitHub Releases instead)
# mobile_app/build/app/outputs/flutter-apk/*.apk
